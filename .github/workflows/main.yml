name: YouTube Live M3U8 Generator
run-name: Generate playlist with cookies 🎬

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */2 * * *'  # Run every 2 hours (reduced frequency)
  push:
    paths:
      - 'streams.txt'
      - '.github/workflows/main.yml'

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install yt-dlp and Deno
        run: |
          # Update pip and install yt-dlp
          pip install --upgrade pip
          pip install --upgrade yt-dlp
          
          # Install Deno properly
          echo "📦 Installing Deno..."
          curl -fsSL https://deno.land/install.sh | sh
          
          # Add Deno to PATH
          DENO_PATH="/home/runner/.deno/bin"
          echo "$DENO_PATH" >> $GITHUB_PATH
          export PATH="$DENO_PATH:$PATH"
          
          # Verify installations
          echo ""
          echo "✅ yt-dlp version: $(yt-dlp --version)"
          echo "✅ Deno version: $(deno --version | head -1)"
          echo "✅ Deno location: $(which deno)"

      - name: Create cookies file
        env:
          COOKIES: ${{ secrets.COOKIES_TXT }}
        run: |
          if [ -n "$COOKIES" ]; then
            echo "$COOKIES" > cookies.txt
            echo "✅ Cookies file created ($(wc -l < cookies.txt) lines)"
          else
            echo "⚠️ No cookies secret found"
            exit 1
          fi

      - name: Generate playlist with rate limit protection
        run: |
          echo "#EXTM3U" > streams.m3u8
          echo "# Generated: $(date -u)" >> streams.m3u8
          echo "# Note: Added delays to avoid YouTube rate limiting" >> streams.m3u8
          echo "" >> streams.m3u8

          TOTAL=0
          ACTIVE=0

          while IFS= read -r line || [ -n "$line" ]; do
            [[ "$line" =~ ^#.*$ ]] && continue
            [[ -z "${line// }" ]] && continue

            URL=$(echo "$line" | grep -o 'https\?://[^ ]*' | head -1)
            [ -z "$URL" ] && continue

            TOTAL=$((TOTAL + 1))
            echo ""
            echo "📺 [$TOTAL] Processing: $URL"

            NAME=$(echo "$URL" | grep -o '@[^/]*' | tr -d '@' || echo "Channel $TOTAL")

            STREAM_URL=""

            if [ -f "cookies.txt" ]; then
              echo "  🔑 Using cookies..."
              
              # CRITICAL: Add random delay to avoid rate limiting
              DELAY=$((RANDOM % 15 + 10))  # Random delay between 10-25 seconds
              echo "  ⏱️ Rate limit protection: Waiting $DELAY seconds before request..."
              sleep $DELAY
              
              # Get video ID with timeout
              echo "  🔍 Fetching video ID..."
              VIDEO_ID=$(yt-dlp --cookies cookies.txt --socket-timeout 20 --get-id "$URL" 2>/dev/null)
              
              if [ -n "$VIDEO_ID" ]; then
                echo "  ✅ Got video ID: $VIDEO_ID"
                
                # Add another small delay before format requests
                sleep 3
                
                # Try formats in order of quality
                for fmt in 301 300 best; do
                  echo "    Trying format $fmt..."
                  STREAM_URL=$(yt-dlp --cookies cookies.txt --socket-timeout 20 -f $fmt -g "https://youtube.com/watch?v=$VIDEO_ID" 2>/dev/null | head -1)
                  if [ -n "$STREAM_URL" ]; then
                    echo "    ✅ Got format $fmt"
                    break
                  fi
                  # Small delay between format attempts
                  sleep 2
                done
              else
                echo "  ❌ Could not get video ID - may be rate limited"
                # If rate limited, wait longer before next attempt
                echo "  ⏱️ Rate limit detected: Waiting 60 seconds..."
                sleep 60
              fi
            fi

            if [ -n "$STREAM_URL" ]; then
              echo "  ✅ Got direct stream URL!"
              ACTIVE=$((ACTIVE + 1))
              echo "" >> streams.m3u8
              echo "#EXTINF:-1,$NAME (Live)" >> streams.m3u8
              echo "$STREAM_URL" >> streams.m3u8
            else
              echo "  ⚠️ Using channel URL as fallback"
              echo "" >> streams.m3u8
              echo "#EXTINF:-1,$NAME (via YouTube)" >> streams.m3u8
              echo "$URL" >> streams.m3u8
            fi

            # CRITICAL: Long delay between channels
            if [ $TOTAL -lt 3 ]; then
              echo "  ⏱️ Channel cooldown: Waiting 30 seconds before next channel..."
              sleep 30
            fi

          done < streams.txt

          echo "" >> streams.m3u8
          echo "# Summary: $ACTIVE/$TOTAL direct streams" >> streams.m3u8
          echo "# Updated: $(date -u)" >> streams.m3u8
          
          # Clean up
          rm -f cookies.txt
          
          echo ""
          echo "📊 FINAL SUMMARY: $ACTIVE/$TOTAL streams have direct URLs"
          echo "✅ Playlist generation complete"

      - name: Show playlist
        run: |
          echo ""
          echo "📄 Generated streams.m3u8:"
          echo "=========================="
          cat streams.m3u8

      - name: Commit and push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git add streams.m3u8
          
          if git diff --staged --quiet; then
            echo "ℹ️ No changes to commit"
            exit 0
          fi
          
          git commit -m "🔄 Update playlist [$(date -u +%H:%M)]"
          git pull --rebase origin main || true
          git push origin main
          echo "✅ Playlist updated and pushed"
